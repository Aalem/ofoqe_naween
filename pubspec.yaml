name: ofoqe_naween
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.27.2
  cupertino_icons: ^1.0.6
  firebase_core:
  firebase_auth:
  firebase_cloud_firestore:
  font_awesome_flutter: ^10.8.0
  provider:
  responsive_framework: ^1.4.0
  sidebarx: ^0.17.1
  flutter_svg:
  flutter_localizations:
    sdk: flutter
  intl:
  dari_datetime_picker:
    git:
      url: https://github.com/Aalem/afghan-datetime-picker.git
      ref: 1303c37d078e21286f595eb4e20722752f9d13dd

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


flutter:
  uses-material-design: true

  assets:
    - assets/images/logo_light.svg